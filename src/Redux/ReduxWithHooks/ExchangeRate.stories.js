import React from 'react';
import { Exchange } from './Exchange';

export default {
  title: 'Redux/ExchangeRateRedux',
  component: Exchange,
};

const Template = (args) => <Exchange {...args} />;

export const ExchangeRateRedux = Template.bind({});
