import React from 'react';
import SearchBar from './search-bar';

export default {
  title: 'Components/Searchhook',
  component: SearchBar,
};

const Template = (args) => <SearchBar {...args} />;

export const Searchhook = Template.bind({});
