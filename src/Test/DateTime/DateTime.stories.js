import React from 'react';
import { Main } from './DateTime';

export default {
  title: 'DateTime',
  component: Main,
};

const Template = (args) => <Main {...args} />;

export const Todos = Template.bind({});
